# open-compliance-library — public GitHub-bound checklist library

Local staging for the PUBLIC GitHub repo of free plain-language DO-178B/C checklists.
`README.md` here is the public-facing README — keep it public-clean. `PUBLISH_STEPS.md`
is the owner's publish runbook; publish handoff:
`C:\Workspace\checklists\work-in-progress\corpus-intake\handoff-prompts\24_public-tier-publish_OPUS.md`.

Hard rules: ONLY public-tier `*_Public_Checklist.{md,json}` exports (copied from the
checklists repo) land in `checklists\` here; confirm the confidentiality wall on every
file before any push. Nothing else in C:\Workspace is public.

Provenance: split out of the checklists repo (`content\open-compliance-library`)
2026-07-12; prior history in the checklists repo through `eff12db`.
